# Deep-Briefing — Python 의존성
# 설치: pip install -r requirements.txt

requests>=2.28.0        # HTTP 클라이언트 (API 호출)
pyyaml>=6.0             # YAML 출력 포맷
python-dotenv>=1.0.0    # .env 파일에서 API 키 로드
matplotlib>=3.7.0       # 차트/그래프 렌더링 (generate-charts.py)
markdown>=3.5           # MD → HTML 변환 (render-report-html.py, Phase 4.7)
jinja2>=3.1             # HTML 템플릿 엔진 (render-report-html.py, Phase 4.7)
# weasyprint는 선택 — Chrome headless가 기본. 시스템에 Chrome 없을 때만 pip install weasyprint

